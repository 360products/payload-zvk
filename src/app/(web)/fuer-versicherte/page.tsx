import StubPage from '@/components/StubPage';

export default function Page() {
  return <StubPage title="Für Versicherte" crumbs={[{ label: "Start", href: "/" }, { label: "Für Versicherte" }]} />;
}
